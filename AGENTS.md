# AGENTS.md

## Cursor Cloud specific instructions

This repository (`plus-marketing-website`) is currently an empty/freshly initialized project. As of the initial commit, it contains only a `README.md` file.

- **No tech stack has been chosen yet.** There are no dependency manifests (`package.json`, `requirements.txt`, etc.), no build configuration, and no source code.
- **No services to run.** There is no application, no dev server, and no database.
- **No tests or linting.** There are no test frameworks or linting tools configured.

Once the project is scaffolded with a framework and dependencies, this section should be updated with:
- How to install dependencies (e.g., `npm install`, `pnpm install`)
- How to run the dev server
- How to run lint and tests
- Any non-obvious caveats discovered during setup
